'use strict';

import './index.styl';

import React from 'react';

const Sidebar = (props) => {
    return (
        <div id='sidebar'>
            <div className='panel'></div>
        </div>
    );
}

export default Sidebar;
